<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="http://appconfig.aura.nav.no"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://appconfig.aura.nav.no
             http://maven.adeo.no/xsd/appconfig.xsd ">

    <name>veilarbperson</name>
    <artifacts>
        <ear groupId="no.nav.fo" artifactId="veilarbperson-ear"/>
    </artifacts>

    <security serviceUserResourceAlias="srvveilarbperson">
        <logins />
    </security>

    <abacSecurity serviceUserResourceAlias="srvveilarbperson"/>

    <selftest>
        <path>veilarbperson/internal/selftest</path>
    </selftest>

    <resources>
        <applicationProperties alias="veilarbperson.properties"/>
        <rest alias="veilarblogin.redirect-url" mapToProperty="oidc-redirect" />
        <webservice alias="virksomhet:Person_v2" mapToProperty="person.endpoint" />
        <webservice alias="virksomhet:EgenAnsatt_v1" mapToProperty="egenansatt.endpoint" />
        <webservice alias="virksomhet:OrganisasjonEnhet_v1" mapToProperty="organisasjonenhet.endpoint" />
        <webservice alias="virksomhet:DigitalKontakinformasjon_v1" mapToProperty="digitalkontaktinformasjon.endpoint" />
        <webservice alias="virksomhet:Kodeverk_v2" mapToProperty="kodeverk.endpoint" />
        <applicationCertificate alias="srvveilarbperson" />
        <rest alias="abac.pdp.endpoint" mapToProperty="abac.endpoint" />
        <baseUrl alias="isso-host"/>
        <baseUrl alias="isso-jwks"/>
        <baseUrl alias="isso-issuer"/>
        <baseUrl alias="isso.isalive"/>
        <credential alias="isso-rp-user"/>
        <ldap alias="ldap" mapToProperty="ldap"/>
    </resources>

    <exposed-services>
        <exposedRest path="/veilarbperson/tjenester" name="veilArbPersonAPI"/>
    </exposed-services>

    <serverOptions>
        <memoryParameters resourceAlias="veilarb-memparams"/>
    </serverOptions>

    <loadBalancer isAlive="/veilarbperson/internal/isAlive" />

    <logging serviceCalls="true" />

</application>